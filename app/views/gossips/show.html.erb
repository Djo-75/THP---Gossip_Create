<html>
  <head>
    <title>The Gossip Project</title>
  </head>

  <body>
  <div class="container">
    <br>
    <h1>BIENVENUE !</h1>
    <br>
    <div class="card">
      <div class="card-header">
        Voici l'index du potin n°<%= @gossip_id.id %>
      </div>
      <div class="card-body">
        <h5 class="card-title">Le titre du gossip est <%= @gossip_id.title %></h5><br>
        <p class="card-text">Le nom de son auteur est le suivant : <%= @gossip_id.user.first_name %></p>
        <p class="card-text">Son contenu est le suivant : <%= @gossip_id.content %></p>
        <p class="card-text">Sa ville est la suivante : <%= @gossip_id.user.city.name %></p>
        <% name_user = @gossip_id.user.first_name %>
        <a href="/users/<%= @gossip_id.user.id %>"class="btn btn-primary">Voir le profil de son auteur</a>
        <a href="/cities/<%= @gossip_id.user.id %>"class="btn btn-primary">Voir la ville de l'auteur</a>
        <a href="/gossips/<%= params[:id] %>/edit"class="btn btn-danger">Editer le potin</a>
        <a href="/gossips/<%= link_to "Détruire", @gossip, data: { turbo_method: :delete, 
                                                                    confirm: "T'es sûr?" } %>
      </div>
    </div>
  </div>
  </body>

</html>
